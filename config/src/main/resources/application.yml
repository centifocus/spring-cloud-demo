server:
  port: 8888


spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/centifocus/spring-cloud-demo
          searchPaths: .config-respo          # git repository path
          default-label: master               # /{application}/{profile}[/{label}] 若label没有，则采用这里的值
          username:
          password:

  application:
    name: config