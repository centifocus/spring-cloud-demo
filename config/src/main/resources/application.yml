
server:
  port: 8888


spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/centifocus/spring-cloud-demo
          searchPaths: .config-respo          # git path
          default-label: master               # branch
          username: centifocus
          password: misaki2850


spring:
  application:
    name: config