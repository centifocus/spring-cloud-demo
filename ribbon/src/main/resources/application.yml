server:
  port: 9931

#
management:
  security:
    enabled: false


eureka:
  instance:
    metadata-map:
      zone: zone1
  client:
    preferSameZoneEureka: true
    serviceUrl:
      defaultZone: http://dns1:9901/eureka/


spring:
  cloud:
    inetutils:
      # 过滤docker的虚拟ip
      ignoredInterfaces:
        - docker0
        - veth.*

    loadbalancer:
      retry:
        enabled: true

  zipkin:
    base-url: http://localhost:9951       # zipkin 控制台

  application:
    name: ribbon
