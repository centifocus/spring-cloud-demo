server:
  port: 8769

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

zuul:
  routes:
    api-hi:
      path: /hi/**
      serviceId: ribbon
    api-oh:
      path: /oh/**
      serviceId: feign

spring:
  application:
    name: zuul

